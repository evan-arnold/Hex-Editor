using System.Drawing;

namespace SHex
{
    public class Variables
    {
        public static Color Background = Color.Black, Foreground = Color.White, InfoText = Color.Gray,
            HighlightBack = Color.Gray, HighlightFore = Color.White;
        public static bool InfoVisible = true, StringsVisible = true;

        public static string HexBoxText, FilePath;
    }
}